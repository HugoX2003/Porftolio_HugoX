---
import { Image } from "astro:assets";
import profileImg from "../assets/profile.webp";
---

<section id="about" class="about-section">
    <div class="container about-content">
        <div class="about-text">
            <h2 class="section-title">
                <span class="bracket">>></span> ABOUT_ME
            </h2>
            <p>
                I am currently a <span class="highlight"
                    >Computer and Systems Engineering</span
                > student (10th cycle) at UPAO in Trujillo, Peru. I specialize in
                <span class="highlight">Frontend Development</span> with a strong
                eye for design and user experience, while maintaining solid competence
                in Backend technologies and <span class="highlight"
                    >currently expanding my skills in Cloud/DevOps.</span
                >
            </p>
            <p class="freelance-status">
                Available for freelance work and collaboration.
            </p>

            <div class="specs">
                <div class="spec-item">
                    <span class="label">Architecture:</span>
                    <span class="value">Fullstack</span>
                </div>
                <div class="spec-item">
                    <span class="label">Language:</span>
                    <span class="value">English (C1), Spanish (Native)</span>
                </div>
                <div class="spec-item">
                    <span class="label">Location:</span>
                    <span class="value">Peru</span>
                </div>
            </div>
        </div>
        <div class="about-visual">
            <div class="cyber-circle">
                <Image
                    src={profileImg}
                    alt="Hugo Marquez"
                    class="profile-img"
                    loading="lazy"
                    width={250}
                    height={250}
                />
            </div>
        </div>
    </div>
</section>

<style>
    .about-section {
        padding: 8rem 0;
        border-bottom: 1px solid #111;
    }

    .about-content {
        display: flex;
        align-items: center;
        gap: 4rem;
    }

    .about-text {
        flex: 1;
    }

    .section-title {
        color: #fff;
        margin-bottom: 2rem;
        font-size: 2.5rem;
    }

    @media (max-width: 768px) {
        .section-title {
            font-size: 1.8rem;
        }
    }

    .icon {
        color: var(--color-primary);
        margin-right: 0.5rem;
    }

    p {
        color: #bbb;
        line-height: 1.8;
        margin-bottom: 1.5rem;
        font-size: 1.1rem;
    }

    .highlight {
        color: var(--color-primary);
        font-weight: bold;
    }

    .stats-grid {
        margin-top: 2rem;
        background: #000;
        border: 1px dashed var(--color-border);
        padding: 1.5rem;
        display: grid;
        grid-template-columns: 1fr;
        gap: 0.5rem;
    }

    .stat-item {
        display: flex;
        justify-content: space-between;
        font-family: monospace;
        color: #888;
    }

    .stat-item .label {
        color: var(--color-primary);
    }

    .about-visual {
        flex: 1;
        display: flex;
        justify-content: center;
    }

    /* ... cyber-circle styles preserved ... */
    .cyber-circle {
        width: 250px;
        height: 250px;
        border: 2px solid var(--color-text);
        border-radius: 50%;
        position: relative;
        box-shadow: 0 0 30px rgba(0, 255, 65, 0.1);
    }

    .cyber-circle::before {
        content: "";
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 200px;
        height: 200px;
        border: 1px dashed var(--color-primary);
        border-radius: 50%;
        animation: rotate 10s linear infinite;
    }

    .profile-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 50%;
        opacity: 0.9;
        filter: grayscale(100%) contrast(1.2);
        transition: all 0.5s ease;
    }

    .cyber-circle:hover .profile-img {
        filter: grayscale(0%) contrast(1);
        opacity: 1;
    }

    @keyframes rotate {
        from {
            transform: translate(-50%, -50%) rotate(0deg);
        }
        to {
            transform: translate(-50%, -50%) rotate(360deg);
        }
    }

    @media (max-width: 768px) {
        .about-content {
            flex-direction: column-reverse;
            text-align: center;
        }

        .stats-grid {
            text-align: left;
        }
    }
</style>
